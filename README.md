# Homwork11 Note Taker:
This application allows the user to create, view, and delete notes. The notes are stored in a JSON file stored in a db.json file, which the server reads and edits as necessary.
